import { Link, Outlet } from "react-router-dom";

const Patient = () => {
    return (  
        <div>
            
            <Outlet/>
            
        </div>
        );
}
 
export default Patient;